## 改动

- 关联 Issue：Fixes #
- 做了什么、为什么要做：

## 检查

- [ ] 这是代码或文档改动，不是用 PR 报 Bug（报问题请开 Issue）
- [ ] 只覆盖自己的或已获授权的目标；不含检测绕过、DRM、反作弊、支付或访问控制规避
- [ ] 不含第三方 `.deb` / `.dylib` / framework 的捆绑、下载或镜像
- [ ] 已在本地跑过 `swift test`，关键路径手动点过
- [ ] 破坏性操作仍是预览后确认，没有改成默认直接执行
